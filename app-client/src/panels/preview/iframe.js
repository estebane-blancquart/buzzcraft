/**
 * COMMIT 66 - Panel Preview
 * 
 * FAIT QUOI : Module générique avec fonctionnalités de base
 * REÇOIT : config: object, options?: object
 * RETOURNE : { success: boolean, result: object, metadata: object }
 * ERREURS : GenericError si opération échoue, ConfigError si config invalide
 */

// panels/preview/iframe : Panel Preview (commit 66)
// DEPENDENCY FLOW (no circular deps)
